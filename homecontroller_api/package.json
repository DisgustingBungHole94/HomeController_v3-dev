{
  "name": "homecontroller_api",
  "version": "1.0.0",
  "description": "HomeController main API",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Josh Dittmer",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^20.10.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "typescript": "^5.3.2"
  }
}
